# Pollae — 맥락 노트 (AI 필독)

> 개발 중 까먹으면 안 되는 핵심 결정사항 모음.
> 새 대화를 시작할 때마다 이 파일을 먼저 읽으세요.

---

## 📋 목차

| § | 내용 | 키워드 |
|---|---|---|
| 1 | [서비스 핵심 결정](#1-서비스-핵심-결정) | 방향, 차별점, 타겟 |
| 2 | [디자인 결정](#2-디자인-결정) | 테마, 컬러, 이모지, 투표버튼 |
| 3 | [인증 구조](#3-인증-구조) | 호스트, 게스트, 토큰, 중복 |
| 4 | [투표 UX 결정](#4-투표-ux-결정) | 버튼, 제출, 수정, 현황 |
| 5 | [투표 구조 결정](#5-투표-구조-결정) | Poll, schedule, place, custom, 연계투표 |
| 6 | [디자인 파일 위치](#6-디자인-파일-위치) | JSX, HTML, 어디서 참조 |
| 7 | [미결 사항](#7-미결-사항) | 아직 결정 안 된 것 |

---

## 1. 서비스 핵심 결정

**서비스명**: 폴래 (Pollae) — poll(투표) + -래(할래?)
**슬로건**: 우리, 언제 만날까요?
**타겟**: 1020 (10~20대) · 친구/동아리/소모임
**포지셔닝**: Chouseisan(일본)을 주 참고, Doodle 보조 참고. 한국 친화적으로.

**핵심 차별점 vs 경쟁 서비스**
- Chouseisan 대비: 자동 확정, 수정 가능, 모바일 퍼스트 UX, 리마인더 알림
- Doodle 대비: 가입 불필요(게스트), 카카오 공유 친화적, 한국어 톤
- 공통 개선: 오래된 UI → 1020 감성, 알림 없음 → 자동 리마인더

---

## 2. 디자인 결정

**테마**: jellyPeach (확정)
```
메인:   #FF7AA8 (핫핑크)
보조:   #FFB088 (피치)  · #C8A8E9 (라벤더)
참석○:  #FF7AA8
미정△:  #FFA962
불참×:  #9580C0
배경:   #FFF5F0
```

**폰트**: Pretendard (한글/영문 통일, 무료 오픈소스)

**투표 버튼 이모지**: 👍(참석) 🤔(미정) 👎(불참) — 기호(○△×) 아님

**투표 상태 레이블**: 참석 / 미정 / 불참 (애매→미정으로 변경 확정)

**아바타**: 이모지 59종 랜덤 배정 · 이름 기반 아님 · 마운트 시 고정

**디자인 파일**: `design/` 폴더의 클로드 디자인 산출물이 원본.
코드 구현 시 이 파일들을 참조해서 포팅.

---

## 3. 인증 구조

```
호스트: 카카오/구글 소셜 로그인 (Supabase Auth)
게스트: 이름 입력 + 브라우저 토큰 (로컬스토리지)
열람:   링크만 있으면 누구나 가능 (로그인 불필요)
```

**소셜 로그인 전략**

| 플랫폼 | 단계 | 상태 |
|---|---|---|
| 카카오 | 1단계 (지금) | ✅ 구현 |
| 구글 | 1단계 (지금) | ✅ 구현 |
| 네이버 | 2단계 (배포 후) | ⬜ 서비스 URL 필요 |
| 애플 | 3단계 (앱 출시 시) | ⬜ 유료 개발자 계정 $99/년 필요 |

> 카카오: 1020 타겟 필수 · 구글: 글로벌 커버리지
> 네이버/애플은 나중에 Supabase Provider 추가만 하면 됨 (코드 변경 최소)

**게스트 토큰 규칙**
- 키: `pollae_guest_token` (로컬스토리지)
- 첫 접속 시 `crypto.randomUUID()` 생성 후 저장
- 같은 기기 재접속 → 기존 토큰 재사용 → 수정 가능
- 다른 기기 → 다른 토큰 → 중복 의심 플로우

**중복 감지 플로우**
```
같은 이름 + 다른 토큰 → "다른 기기에서 투표하셨나요?" 안내
  └→ "내 거예요" → 호스트 알림 → 호스트 판단 후 삭제
  └→ "다른 사람" → 이름(2) 처리
삭제된 게스트 재접속 → 미투표 상태로 표시 (알림 없음, 게스트 연락수단 없음)
```

**호스트 권한**
- 게스트 투표 삭제 가능
- 마감 전 수동 종료 가능
- 동점 시 최종 날짜 직접 선택
- 비공개 설정에서도 전체 열람 가능

---

## 4. 투표 UX 결정

**슬롯 구조**: 날짜별 카드 묶음 → 시간 행마다 우측에 버튼
```
[5월 27일 (수)]
  오후 7 ~ 8시     5명 응답    👍 🤔 👎
                               4  1  0
  오후 8 ~ 9시     5명 응답    👍 🤔 👎
                               3  2  0
```

**투표 버튼 위치**: 시간 텍스트 오른쪽 (가로 배열)
**버튼 아래 숫자**: 각 버튼 중앙 바로 아래 (심볼 없이 숫자만)
**응답자 수**: 시간 아래에 "N명 응답" 표시

**"투표 완료" 버튼 = 제출 개념** (임시저장 아님)
- ○△× 선택은 로컬 임시 상태
- 버튼 눌러야 서버 전송
- 제출 전 이탈 시 경고
- 제출 후 → "수정하기" 버튼 전환

**"참석 가장 많아요" 배지**: 투표 완료(제출) 후에만 표시
**투표 현황**: 투표 전에도 열람 가능
**슬롯 최대**: 30개 (날짜 × 시간 합산 기준)

**이벤트 생성 레이블 변경**
- "날짜 선택" → "일정 선택" (날짜+시간 모두 포함이라)


---

## 5. 투표 구조 결정

**구조 방향**: 일정 투표가 메인, 장소·커스텀은 선택 연계 투표

```
Event
  └→ Poll[] (투표 라운드)
        └→ PollItem[] (투표 항목)
              └→ Vote[] (응답)
```

**Poll 타입**

| poll_type | 설명 | 필수 여부 |
|---|---|---|
| `schedule` | 일정 투표 (날짜+시간) | ✅ 필수 (첫 번째 Poll) |
| `place` | 장소 투표 (텍스트 항목) | ⬜ 선택 |
| `custom` | 자유 항목 (드레스코드 등) | ⬜ 선택 |

**UX 플로우**
```
이벤트 생성
  └→ 1. 일정 투표 (필수) — 기존과 동일

일정 확정 후 or 생성 시
  └→ [+ 투표 추가] 버튼 (선택)
       ├→ 📍 장소 투표  → 텍스트 항목 직접 입력
       ├→ 👗 드레스코드 → 텍스트 항목 직접 입력
       └→ ✏️ 직접 입력  → 제목 + 항목 자유 입력
```

**투표 항목 (PollItem)**
- `schedule` 타입: slot_date + slot_time + slot_time_end (기존 TimeSlot 구조)
- `place` / `custom` 타입: label (텍스트) + description (선택)

**응답 방식**: 모든 타입 동일하게 ○△× (yes/maybe/no)
**확정 로직**: 동일 (yes 최다 자동 확정, 동점 시 호스트 선택)
**슬롯 제한**: poll_type별로 각각 최대 30개

**기존 설계 변경사항**
- `time_slots` 테이블 → `poll_items` 테이블로 통합
- `events` 테이블에 직접 연결되던 슬롯 → `polls` 테이블 경유
- `TTimeSlot` 타입 → `TPoll`, `TPollItem` 타입으로 교체
- `useVote(eventId)` → `useVote(pollId)` 로 단위 변경

---

## 6. 디자인 파일 위치

```
design/
├── Pollae_Prototype.html       # 전체 플로우 인터랙티브 (랜딩→투표→결과)
├── Pollae_Full_Screens.html    # 모든 화면 하이파이 캔버스
├── Pollae_Vote.html            # 투표 페이지 단독 + Tweaks 패널
├── Pollae_Storybook_Standalone.html  # 디자인 시스템 단독 실행
├── cute-vote.jsx               # CuteVotePage — 투표 페이지 전체
├── cute-create.jsx             # LandingScreen, CreateStep1~3, InviteScreen
│                               # + NavBar, PrimaryBtn, ProgressDots 컴포넌트
├── cute-result.jsx             # ResultScreen, DashboardScreen
├── cute-auth.jsx               # LoginScreen, GuestConflictScreen, HostManageScreen
├── cute-states.jsx             # EmptyDashboardScreen, LoadingScreen, ErrorScreen
├── cute-themes.jsx             # CUTE_THEMES.jellyPeach — 디자인 토큰 원본
├── cute-chars.jsx              # CuteAvatar, EMOJI_AVATARS (59종)
└── demo-users.jsx              # DEMO_USERS 고정 아바타 데이터
```

**코드 포팅 방법**
1. `cute-themes.jsx`의 jellyPeach 토큰값 → `src/lib/theme.ts`에 COLORS로
2. `cute-create.jsx`의 NavBar, PrimaryBtn → `src/components/ui/`로 포팅
3. `cute-vote.jsx`의 CuteVotePage → `src/app/v/[code]/page.tsx` + 관련 컴포넌트로 분해
4. 각 화면의 스타일/레이아웃은 JSX 파일 직접 참조 (디자인 완성본)

---

## 7. 미결 사항

| 항목 | 상태 | 비고 |
|---|---|---|
| 카카오 알림톡 | 유료화 이후 추가 | 현재는 이메일+웹푸시만 |
| 앱 스토어 배포 | 모웹 우선, 이후 검토 | 현재는 PWA |
| 색상 테마 선택 기능 | 기획 포함 | 호스트가 이벤트 생성 시 색상 설정 가능 |
| 정기 모임 템플릿 | 미기획 | 나중에 추가 고려 |
| 캘린더 연동 | 미기획 | Google/Outlook 확정 후 등록 |
